import 'package:flutter/material.dart';
import 'package:logger/logger.dart';

import 'common/cr_colors.dart';

const appTitle = "mAFAIII";

const divider = Divider(color: dividerColor);
const verticalDivider = VerticalDivider(color: dividerColor, width: 2);

final logger = Logger();
